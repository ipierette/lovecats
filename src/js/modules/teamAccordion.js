/**
 * Team Card System
 * Simple static cards - no interaction logic needed
 */

export function initTeamAccordion() {
  // No JavaScript logic needed for static cards
  // Keeping this file for backwards compatibility
  // Can be safely removed if app.js import is also removed
}
